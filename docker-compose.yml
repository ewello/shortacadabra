version: "3.9"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: /bin/sh -c "python manage.py migrate && gunicorn Shortacadabra.wsgi:application --bind 0.0.0.0:8000"


